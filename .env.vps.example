# MM.AI VPS data-root configuration
#
# Keep this identical to MMWeb's production MM_DATA_ROOT value.
# On KVM4/MMWeb this is:
MM_DATA_ROOT=/opt/mm-web-data

# Do not set MM_INSTALL_ROOT to /opt/mm-web-data.
# MM_INSTALL_ROOT is an install folder and MM.AI would append /data to it.
